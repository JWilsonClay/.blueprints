# .blueprints Agent Boot Sequence & Governance Rules

### CRITICAL TOOL RULE — NEVER BREAK THIS:
You have TWO terminal tools:
1. The GOOD one: "Ran background command" / background mode / uses Command ID + command_status polling. ALWAYS use this.
2. The BAD one: "Running command" / synchronous / blocks the agent and hangs. NEVER use this.

For ANY terminal command (find, grep, ls, audit, etc.):
- ALWAYS choose the background version.
- After it runs, you MUST use the "Checked command status" / command_status tool to read output.
- If the UI ever shows "Running.." (no exit code), CANCEL immediately and retry with background mode.

---

### Step 1 — Absolute Import Enforcement
All Python files in the `governance/` directory MUST use absolute imports from the `governance.` namespace. Do not use `contentflow` or other legacy package roots.

---

### Step 2 — Workspace Contextualization (The Doorway)
Execute the following to update the architectural map and check for drift:
```python
import sys; from pathlib import Path; root = "/home/jwils/GoogleDrive1TB/PapiBobes/.blueprints"; sys.path.insert(0, root); from governance.thedoorway.dynamic_contextualizer import DoorwayContextualizer; DoorwayContextualizer().run()
```

---

### Step 3 — Workspace Manifest
[MANIFEST.md](file:///home/jwils/GoogleDrive1TB/PapiBobes/.blueprints/governance/MANIFEST.md)
Contains the registry of all discovered READMEs and domain owners.

---

### Step 5 — Folder Ownership
[FOLDER_OWNERSHIP.md](file:///home/jwils/GoogleDrive1TB/PapiBobes/.blueprints/docs/FOLDER_OWNERSHIP.md)
Source of Truth for directory seniority and agent responsibility.

---

### Step 6 — Substrate Hygiene & Repair
If Step 2 generates a repair plan, view it here:
[repair_implementation_plan.md](file:///home/jwils/GoogleDrive1TB/PapiBobes/.blueprints/governance/repair_implementation_plan.md)
Follow strictly to achieve a Zero-Finding State.
